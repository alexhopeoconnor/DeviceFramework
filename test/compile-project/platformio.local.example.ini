; Copy to platformio.local.ini.<machine> while validating sibling worktrees
; before their public tags exist. This file is ignored by Git. Explicit
; symlinks replace the package tags; lib_extra_dirs alone is not sufficient
; for dependencies declared by DeviceFramework's library manifest. Do not set
; lib_dir to the sibling-worktree parent: it can make PlatformIO discover
; nested test/build directories that are not consumer dependencies.
[common]
lib_deps =
    DeviceFramework=symlink://../..
    WiFiManager=symlink://../../../WiFiManager
    DeviceFrameworkTemplateEngine=symlink://../../../DFTE
    home-assistant-integration=symlink://../../../arduino-home-assistant
    madpilot/mDNSResolver@^0.3.0
    knolleary/PubSubClient@2.8.0
    ESP32Async/ESPAsyncWebServer@^3.12.0
    bblanchon/ArduinoJson@^7.4.3

[env:esp8266]
lib_deps =
    ${common.lib_deps}
    ESP32Async/ESPAsyncTCP@2.0.0

[env:esp32]
lib_deps =
    ${common.lib_deps}
    ESP32Async/AsyncTCP@^3.5.0
