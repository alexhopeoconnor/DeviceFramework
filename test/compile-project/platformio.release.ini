; Used by the board-free release gate. It intentionally excludes ignored local
; overrides, while building the checked-out DeviceFramework package against the
; exact first-party Git tags in its library manifest.
[platformio]
default_envs = esp8266
extra_configs = platformio.base.ini
