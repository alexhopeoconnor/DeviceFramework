; Board-free verification uses this configuration so an ignored sibling-worktree
; override cannot change which released first-party libraries are compiled.
[platformio]
default_envs = esp8266_portal_ota_protected_a
extra_configs = platformio.base.ini

