; Copy to platformio.local.ini.<machine> only when validating unpublished
; sibling worktrees. This file is ignored. It mirrors the regular consumer
; fixture without broad lib_dir discovery, so PlatformIO cannot accidentally
; treat another repository's tests as product dependencies.
[common]
lib_extra_dirs =
    ../../../WiFiManager/lib
lib_deps =
    DeviceFramework=symlink://../..
    WiFiManager=symlink://../../../WiFiManager
    DeviceFrameworkTemplateEngine=symlink://../../../DFTE
    home-assistant-integration=symlink://../../../arduino-home-assistant
    madpilot/mDNSResolver@^0.3.0
    knolleary/PubSubClient@2.8.0
    ESP32Async/ESPAsyncWebServer@^3.12.0
    bblanchon/ArduinoJson@^7.4.3

[env:esp8266_ota_base]
lib_deps =
    ${common.lib_deps}
    ESP32Async/ESPAsyncTCP@2.0.0

[env:esp32_ota_base]
lib_deps =
    ${common.lib_deps}
    ESP32Async/AsyncTCP@^3.5.0

