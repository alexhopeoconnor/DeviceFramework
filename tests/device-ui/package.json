{
  "private": true,
  "name": "deviceframework-device-ui-contract",
  "version": "1.0.0",
  "description": "Docker-only real-board visual contract for DeviceFramework UI surfaces",
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
